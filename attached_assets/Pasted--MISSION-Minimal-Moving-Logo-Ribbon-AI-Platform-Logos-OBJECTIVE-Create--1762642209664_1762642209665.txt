---

## **🎯 MISSION: Minimal Moving Logo Ribbon - AI Platform Logos**

### **📋 OBJECTIVE**
Create an elegant, minimal, continuously moving logo ribbon featuring ChatGPT, Claude, Gemini, and Perplexity logos in the space between the stats section (89% avg improvement) and the video section ("Watch Radius in 30 Seconds"). The logos should be grayscale/faded, move smoothly in an infinite loop, and seamlessly integrate with Radius's premium black-and-white design language. Zero extra padding - fill the existing gap naturally.

---

## **🖼️ REFERENCE & PLACEMENT**

**Current Layout (from image):**
```
[Stats: 500+ analyses | 89% avg improvement | 4 AI platforms]
                          ↓
                   EMPTY SPACE (~150-200px)
                          ↓
              "Watch Radius in 30 Seconds"
                    [Video Player]
```

**New Layout:**
```
[Stats: 500+ analyses | 89% avg improvement | 4 AI platforms]
                          ↓
              🎯 MOVING LOGO RIBBON (NEW)
      [ChatGPT] [Claude] [Gemini] [Perplexity] → → → [Loop]
                          ↓
              "Watch Radius in 30 Seconds"
                    [Video Player]
```

---

## **💻 COMPLETE IMPLEMENTATION CODE**

### **Component: `LogoRibbon.tsx`**

```typescript
import React from 'react';

export const LogoRibbon: React.FC = () => {
  // Logo data with SVG paths or image URLs
  const logos = [
    {
      name: 'ChatGPT',
      svg: (
        <svg viewBox="0 0 24 24" className="w-full h-full" fill="currentColor">
          <path d="M22.282 9.821a5.985 5.985 0 0 0-.516-4.91 6.046 6.046 0 0 0-6.51-2.9A6.065 6.065 0 0 0 4.981 4.18a5.985 5.985 0 0 0-3.998 2.9 6.046 6.046 0 0 0 .743 7.097 5.98 5.98 0 0 0 .51 4.911 6.051 6.051 0 0 0 6.515 2.9A5.985 5.985 0 0 0 13.26 24a6.056 6.056 0 0 0 5.772-4.206 5.99 5.99 0 0 0 3.997-2.9 6.056 6.056 0 0 0-.747-7.073zM13.26 22.43a4.476 4.476 0 0 1-2.876-1.04l.141-.081 4.779-2.758a.795.795 0 0 0 .392-.681v-6.737l2.02 1.168a.071.071 0 0 1 .038.052v5.583a4.504 4.504 0 0 1-4.494 4.494zM3.6 18.304a4.47 4.47 0 0 1-.535-3.014l.142.085 4.783 2.759a.771.771 0 0 0 .78 0l5.843-3.369v2.332a.08.08 0 0 1-.033.062L9.74 19.95a4.5 4.5 0 0 1-6.14-1.646zM2.34 7.896a4.485 4.485 0 0 1 2.366-1.973V11.6a.766.766 0 0 0 .388.676l5.815 3.355-2.02 1.168a.076.076 0 0 1-.071 0l-4.83-2.786A4.504 4.504 0 0 1 2.34 7.872zm16.597 3.855l-5.833-3.387L15.119 7.2a.076.076 0 0 1 .071 0l4.83 2.791a4.494 4.494 0 0 1-.676 8.105v-5.678a.79.79 0 0 0-.407-.667zm2.01-3.023l-.141-.085-4.774-2.782a.776.776 0 0 0-.785 0L9.409 9.23V6.897a.066.066 0 0 1 .028-.061l4.83-2.787a4.5 4.5 0 0 1 6.68 4.66zm-12.64 4.135l-2.02-1.164a.08.08 0 0 1-.038-.057V6.075a4.5 4.5 0 0 1 7.375-3.453l-.142.08L8.704 5.46a.795.795 0 0 0-.393.681zm1.097-2.365l2.602-1.5 2.607 1.5v2.999l-2.597 1.5-2.607-1.5z"/>
        </svg>
      ),
    },
    {
      name: 'Claude',
      svg: (
        <svg viewBox="0 0 24 24" className="w-full h-full" fill="currentColor">
          <path d="M14.154 1.443c-.837-1.142-2.47-1.142-3.308 0L1.21 13.326c-.838 1.142-.193 2.674 1.154 2.674h3.077l-2.615 5.623c-.448.962.49 1.943 1.452 1.496l16.538-7.692c.963-.448.963-1.944 0-2.392l-6.154-2.77 6.154-2.77c.963-.448.963-1.944 0-2.392L14.154 1.443z"/>
        </svg>
      ),
    },
    {
      name: 'Gemini',
      svg: (
        <svg viewBox="0 0 24 24" className="w-full h-full" fill="currentColor">
          <path d="M12 0L9.798 8.697 1.101 10.899 9.798 13.101 12 21.798 14.202 13.101 22.899 10.899 14.202 8.697z"/>
          <circle cx="12" cy="12" r="3"/>
        </svg>
      ),
    },
    {
      name: 'Perplexity',
      svg: (
        <svg viewBox="0 0 24 24" className="w-full h-full" fill="currentColor">
          <rect x="2" y="2" width="9" height="9" rx="1"/>
          <rect x="13" y="2" width="9" height="9" rx="1"/>
          <rect x="2" y="13" width="9" height="9" rx="1"/>
          <rect x="13" y="13" width="9" height="9" rx="1"/>
        </svg>
      ),
    },
  ];

  // Duplicate logos for seamless infinite scroll
  const duplicatedLogos = [...logos, ...logos, ...logos];

  return (
    <div className="w-full py-12 md:py-16 overflow-hidden bg-white">
      <div className="relative">
        {/* Gradient fade on edges */}
        <div className="absolute left-0 top-0 bottom-0 w-32 bg-gradient-to-r from-white to-transparent z-10" />
        <div className="absolute right-0 top-0 bottom-0 w-32 bg-gradient-to-l from-white to-transparent z-10" />
        
        {/* Moving logo container */}
        <div className="flex animate-scroll-logos">
          {duplicatedLogos.map((logo, index) => (
            <div
              key={`${logo.name}-${index}`}
              className="flex-shrink-0 mx-8 md:mx-12 w-16 h-16 md:w-20 md:h-20 text-gray-300 opacity-40 hover:opacity-60 transition-opacity duration-300"
              aria-label={logo.name}
            >
              {logo.svg}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};
```

---

## **🎨 REQUIRED CSS ANIMATION**

Add this to your `globals.css` or Tailwind config:

```css
/* Infinite scrolling animation */
@keyframes scroll-logos {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-33.333%);
  }
}

.animate-scroll-logos {
  animation: scroll-logos 30s linear infinite;
  display: flex;
  width: max-content;
}

/* Pause on hover (optional) */
.animate-scroll-logos:hover {
  animation-play-state: paused;
}
```

**OR in Tailwind config:**

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      keyframes: {
        'scroll-logos': {
          '0%': { transform: 'translateX(0)' },
          '100%': { transform: 'translateX(-33.333%)' },
        },
      },
      animation: {
        'scroll-logos': 'scroll-logos 30s linear infinite',
      },
    },
  },
};
```

---

## **🎨 DESIGN SPECIFICATIONS**

### **Logo Styling**

**Color & Opacity:**
- Base color: `#D1D5DB` (gray-300)
- Opacity: `0.4` (40%) - faded, subtle
- Hover opacity: `0.6` (60%) - slightly more visible
- Transition: `300ms ease`

**Size:**
- Mobile: `64px × 64px` (w-16 h-16)
- Desktop: `80px × 80px` (w-20 h-20)

**Spacing:**
- Horizontal gap between logos: `64px` (mobile), `96px` (desktop)
- mx-8 md:mx-12

### **Container Styling**

**Section:**
- Background: Pure white (`#FFFFFF`)
- Vertical padding: `48px` (mobile), `64px` (desktop)
- No horizontal padding (fills width)
- Overflow: Hidden (hides logos outside viewport)

**Edge Gradients:**
- Left fade: `bg-gradient-to-r from-white to-transparent`
- Right fade: `bg-gradient-to-l from-white to-transparent`
- Width: `128px` on each side
- Purpose: Smooth fade-in/out as logos scroll

### **Animation**

**Movement:**
- Direction: Right to left (→ ←)
- Speed: 30 seconds for full cycle
- Easing: Linear (constant speed)
- Loop: Infinite
- Seamless: Uses 3x duplication (scrolls 33.333%)

**Behavior:**
- Continuous motion, never stops
- Optional: Pause on hover
- No janky starts/stops
- Perfectly smooth loop

---

## **📱 RESPONSIVE BEHAVIOR**

### **Desktop (1024px+)**
- Logo size: 80px × 80px
- Spacing: 96px between logos
- Full padding: 64px vertical
- All logos clearly visible

### **Tablet (768px - 1023px)**
- Logo size: 72px × 72px
- Spacing: 80px between logos
- Padding: 56px vertical

### **Mobile (320px - 767px)**
- Logo size: 64px × 64px
- Spacing: 64px between logos
- Padding: 48px vertical
- Fewer logos visible at once

---

## **🎨 LOGO SVG ALTERNATIVES**

If you need simpler placeholder logos:

```typescript
const logos = [
  {
    name: 'ChatGPT',
    svg: (
      <svg viewBox="0 0 100 100" className="w-full h-full" fill="currentColor">
        <circle cx="50" cy="50" r="45" strokeWidth="5" stroke="currentColor" fill="none"/>
        <text x="50" y="58" fontSize="20" textAnchor="middle" fontWeight="bold">GPT</text>
      </svg>
    ),
  },
  {
    name: 'Claude',
    svg: (
      <svg viewBox="0 0 100 100" className="w-full h-full" fill="currentColor">
        <rect x="10" y="10" width="80" height="80" rx="15" fill="none" stroke="currentColor" strokeWidth="5"/>
        <text x="50" y="58" fontSize="18" textAnchor="middle" fontWeight="bold">CL</text>
      </svg>
    ),
  },
  {
    name: 'Gemini',
    svg: (
      <svg viewBox="0 0 100 100" className="w-full h-full" fill="currentColor">
        <path d="M50,10 L90,50 L50,90 L10,50 Z" fill="none" stroke="currentColor" strokeWidth="5"/>
        <text x="50" y="58" fontSize="18" textAnchor="middle" fontWeight="bold">GM</text>
      </svg>
    ),
  },
  {
    name: 'Perplexity',
    svg: (
      <svg viewBox="0 0 100 100" className="w-full h-full" fill="currentColor">
        <polygon points="50,15 85,85 15,85" fill="none" stroke="currentColor" strokeWidth="5"/>
        <text x="50" y="63" fontSize="18" textAnchor="middle" fontWeight="bold">PX</text>
      </svg>
    ),
  },
];
```

---

## **🔗 INTEGRATION INTO MAIN PAGE**

```typescript
import { LogoRibbon } from '@/components/LogoRibbon';
import { VideoSection } from '@/components/VideoSection';

export default function HomePage() {
  return (
    <div className="min-h-screen bg-white">
      
      {/* Navbar */}
      <nav>{/* ... */}</nav>

      {/* Hero / Search Section */}
      <section className="pt-32 pb-20 px-6">
        {/* Headline, search bar, stats */}
        
        {/* Stats - existing */}
        <div className="flex flex-wrap justify-center items-center gap-8 text-sm text-gray-600">
          <div className="flex items-center gap-2">
            <span className="w-2 h-2 bg-green-500 rounded-full animate-pulse" />
            <span>500+ analyses run</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="font-semibold text-gray-900">89%</span>
            <span>avg improvement</span>
          </div>
          <div className="flex items-center gap-2">
            <span className="font-semibold text-gray-900">4</span>
            <span>AI platforms analyzed</span>
          </div>
        </div>
      </section>

      {/* 🎯 LOGO RIBBON - ADD HERE (NO EXTRA PADDING) */}
      <LogoRibbon />

      {/* Video Section */}
      <VideoSection />

      {/* Features Section */}
      {/* ... */}
    </div>
  );
}
```

---

## **⚡ PERFORMANCE OPTIMIZATIONS**

### **1. CSS-Only Animation**
```css
/* Use CSS transforms for GPU acceleration */
.animate-scroll-logos {
  will-change: transform;
  backface-visibility: hidden;
}
```

### **2. Reduce Motion for Accessibility**
```css
@media (prefers-reduced-motion: reduce) {
  .animate-scroll-logos {
    animation: none;
  }
}
```

### **3. Lazy Load SVGs**
```typescript
// If using image files instead of inline SVGs
<img 
  src="/logos/chatgpt.svg" 
  loading="lazy"
  alt="ChatGPT"
/>
```

---

## **🎨 ALTERNATIVE: SLOWER, MORE ELEGANT VERSION**

For even more premium feel:

```typescript
// Slower animation (45 seconds instead of 30)
animation: scroll-logos 45s linear infinite;

// Larger logos with more spacing
className="w-24 h-24 mx-16"  // 96px logos, 128px spacing

// Even more faded
className="opacity-30 hover:opacity-50"
```

---

## **🎯 VARIATIONS**

### **Option A: Minimal (Recommended)**
- Gray-300 color
- 40% opacity
- 30s animation
- 80px logos
- 96px spacing

### **Option B: Ultra-Subtle**
- Gray-200 color
- 25% opacity
- 45s animation (slower)
- 64px logos
- 128px spacing

### **Option C: Slightly Visible**
- Gray-400 color
- 50% opacity
- 25s animation (faster)
- 96px logos
- 80px spacing

---

## **✅ IMPLEMENTATION CHECKLIST**

- [ ] Create `LogoRibbon.tsx` component
- [ ] Add 4 logo SVGs (ChatGPT, Claude, Gemini, Perplexity)
- [ ] Duplicate logos 3x for seamless loop
- [ ] Implement infinite scroll animation
- [ ] Set logos to gray-300 with 40% opacity
- [ ] Add edge gradient fades
- [ ] Set animation speed to 30 seconds
- [ ] Remove all extra padding (py-12 to py-16 only)
- [ ] Add CSS animation to globals.css
- [ ] Position between stats and video section
- [ ] Test smooth scrolling loop
- [ ] Verify no janky transitions
- [ ] Test hover pause (optional)
- [ ] Check responsive sizing
- [ ] Test on all devices
- [ ] Verify accessibility (reduced motion)
- [ ] Cross-browser testing

---

## **🚀 FINAL IMPLEMENTATION INSTRUCTION**

**Provide this EXACT instruction to Replit Agent:**

"**Task:** Add a minimal, continuously moving logo ribbon featuring AI platform logos.

**CRITICAL PLACEMENT:**
- Position BETWEEN the stats section (89% avg improvement) and the video section
- Should fill the existing empty space with NO extra padding
- Direct sequence: Stats → Logo Ribbon → Video Section

**Requirements:**

1. **Logos (4 AI Platforms):**
   - ChatGPT, Claude, Gemini, Perplexity
   - Use simple SVG representations (can be text-based or geometric)
   - Color: Gray-300 (#D1D5DB)
   - Opacity: 40% (0.4) - very faded and subtle
   - Size: 64px (mobile), 80px (desktop)
   - Spacing: 64px (mobile), 96px (desktop) between logos

2. **Animation:**
   - Infinite horizontal scroll (right to left)
   - Speed: 30 seconds for complete cycle
   - Easing: Linear (constant speed)
   - Seamless loop: Duplicate logos 3x, scroll 33.333%
   - Optional: Pause animation on hover

3. **Container:**
   - Full width, overflow hidden
   - Background: White
   - Vertical padding ONLY: 48px (mobile), 64px (desktop)
   - NO horizontal padding
   - Edge gradients: 128px white-to-transparent fades on left/right

4. **Style:**
   - Ultra-minimal, professional
   - Matches Radius black/white design
   - Faded logos (NOT bright)
   - Smooth, elegant motion
   - No jarring transitions

5. **Animation CSS:**
   ```css
   @keyframes scroll-logos {
     0% { transform: translateX(0); }
     100% { transform: translateX(-33.333%); }
   }
   
   .animate-scroll-logos {
     animation: scroll-logos 30s linear infinite;
   }
   ```

**Integration:** Use the complete `LogoRibbon.tsx` code provided. Place component immediately after the stats section and before `<VideoSection />`.

**Critical:** The ribbon must be MINIMAL and SUBTLE. Logos should be barely visible (gray-300, 40% opacity), creating a premium subliminal effect rather than demanding attention. The motion should be smooth and continuous with no stops or jerks. Zero extra padding beyond what's specified."

---