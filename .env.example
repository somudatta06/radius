# Radius GEO Platform — Environment Variables
# Copy this file to .env and fill in your values.
# The app will run in demo mode (realistic fake data) without OPENAI_API_KEY.

# ── Required for live AI analysis ──────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Optional: MongoDB (defaults to localhost if not set) ───────────────────
MONGO_URL=mongodb://localhost:27017

# ── Optional: Frontend origin (for CORS in production) ─────────────────────
# FRONTEND_URL=https://your-domain.com
